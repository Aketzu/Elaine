xml.data() do
  for playlist in @playlists
    xml.event do
      xml.start(playlist.start_time)
      xml.end(playlist.end_time)
      xml.isDuration(true)
      xml.title(playlist.Program.title)
    end
  end
end
